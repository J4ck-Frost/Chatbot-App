name: CI/CD - Chatbot App (Helm)

on:
  push:
    branches: [ "team-1/long_ngo" ] # Chạy khi push vào nhánh này
    paths:
      - 'Team 1/chatbot-app/**'
      - 'Team 1/helms/chatbot-app/**'
  workflow_dispatch:

env:
  PROJECT_ID: ${{ secrets.TEAM1_GCP_PROJECT }}
  GAR_REPO: asia-southeast1-docker.pkg.dev/${{ secrets.TEAM1_GCP_PROJECT }}/team1-ai-repo
  IMAGE_NAME: chatbot-app
  SOURCE_PATH: "./Team 1/chatbot-app"
  HELM_PATH: "./Team 1/helms/chatbot-app"

jobs:
  build-deploy-chatbot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        # [ĐÃ SỬA] Xóa 'ref: team-1-branch' để nó checkout đúng code mới nhất

      # --- 1. Auth & Setup ---
      - name: Auth GCP
        uses: google-github-actions/auth@v1
        with:
          credentials_json: ${{ secrets.TEAM1_GCP_SA_KEY }}

      - name: Configure Docker
        run: gcloud auth configure-docker asia-southeast1-docker.pkg.dev

      - name: Get GKE Credentials
        uses: google-github-actions/get-gke-credentials@v1
        with:
          cluster_name: ${{ secrets.TEAM1_GKE_CLUSTER }}
          location: ${{ secrets.TEAM1_GKE_LOCATION }}
          project_id: ${{ secrets.TEAM1_GCP_PROJECT }}

      # --- 2. Build & Push Image ---
      - name: Build & Push
        env:
          IMAGE_TAG: ${{ github.sha }}
        run: |
          FULL_IMAGE="$GAR_REPO/$IMAGE_NAME:$IMAGE_TAG"
          docker build -t "$FULL_IMAGE" "$SOURCE_PATH"
          docker push "$FULL_IMAGE"

      # --- 3. Deploy Helm ---
      - name: Install Helm
        uses: azure/setup-helm@v3

      - name: Deploy Chatbot via Helm
        env:
          IMAGE_TAG: ${{ github.sha }}
        run: |
          helm upgrade --install chatbot-app "$HELM_PATH" \
            -n "${{ secrets.TEAM1_K8S_NAMESPACE }}" \
            --set image.repository="$GAR_REPO/$IMAGE_NAME" \
            --set image.tag="$IMAGE_TAG" \
            --wait --timeout 5m